#!/usr/bin/env bash
# Installs nginx server
apt-get update
apt-get -y install nginx
ufw allow 'Nginx HTTP'
echo "Hello World!" > /var/www/html/index.html
if ! lsof -i :80 >/dev/null 2>&1; then
    echo "Nginx is not listening on port 80. Starting Nginx..."
    nginx
fi
